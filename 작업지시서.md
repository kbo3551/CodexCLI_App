Windows에서 사용할 가볍고 완성도 높은 Codex 데스크톱 애플리케이션을 만들어줘.

이 앱의 핵심 목적은 OpenAI API를 직접 호출하는 별도의 AI 클라이언트를 만드는 것이 아니다.

현재 내 PC에서는 **Codex CLI가 WSL Ubuntu 환경에서 매우 빠르고 안정적으로 동작하고 있으며**, 이 기존 Codex CLI 환경을 그대로 사용하면서 Windows에서 실행할 수 있는 GUI 애플리케이션을 만들고 싶다.

내가 Java에 익숙하므로 애플리케이션은 **Java 기반으로 구현**한다.

최종적으로는 Windows에서 일반 데스크톱 앱처럼 실행되지만, 실제 Codex 작업은 기존 WSL Ubuntu 안의 Codex CLI가 수행하도록 만든다.

전체 구조는 다음과 같다.

```text
Windows 11
│
├─ CodexDesktop.exe
│   │
│   ├─ Java 21
│   ├─ JavaFX
│   ├─ 프로젝트 관리
│   ├─ 채팅 UI
│   ├─ Tool Call UI
│   ├─ Diff UI
│   └─ 설정
│
│        stdin / stdout
│        JSONL / RPC
│             ↕
│
├─ wsl.exe
│      │
│      ▼
│   Ubuntu WSL
│      │
│      └─ codex app-server --stdio
│             │
│             ├─ 기존 Codex 로그인
│             ├─ ~/.codex/config.toml
│             ├─ 기존 sandbox 정책
│             ├─ 기존 approval 정책
│             ├─ bash
│             ├─ git
│             └─ 프로젝트 파일 접근
│
└─ Windows 프로젝트 경로
       ↕
   /mnt/c/... 경로로 WSL에서 접근
```

# 핵심 원칙

반드시 다음 조건을 지켜줘.

* Java 기반으로 구현
* JavaFX 사용
* Swing 사용 금지
* OpenAI API Key 사용 금지
* Responses API 직접 호출 금지
* Chat Completions API 직접 호출 금지
* 별도의 자체 AI 인증 구현 금지
* 현재 WSL의 `codex login` 상태를 그대로 활용
* 기존 `~/.codex/config.toml` 설정 활용
* 기존 Codex sandbox 정책 활용
* 기존 Codex approval 정책 활용
* Codex 자체를 수정하지 말 것
* Codex 보안/승인 정책을 우회하지 말 것
* Electron 사용 금지
* 전체 UI를 WebView 기반으로 구현하지 말 것
* 터미널 화면을 그대로 GUI에 임베드하는 방식으로 만들지 말 것

핵심은 **Codex를 새로 만드는 것이 아니라 WSL에서 실행되는 기존 Codex CLI 위에 JavaFX GUI를 얹는 것**이다.

# 기술 스택

다음 기술 스택을 기본으로 사용해줘.

```text
Java 21
JavaFX
Maven
Jackson
SLF4J
Logback
JUnit 5
```

필요한 경우 적절한 라이브러리를 추가해도 된다.

단, 의미 없이 무거운 프레임워크를 추가하지 않는다.

Spring Boot는 사용하지 않는다.

이 앱은 서버 애플리케이션이 아니라 데스크톱 클라이언트이므로 순수 Java 애플리케이션으로 구성한다.

# 빌드 및 패키징

Maven 프로젝트로 구성한다.

개발 중에는 다음과 같이 실행 가능해야 한다.

```bash
mvn clean javafx:run
```

최종적으로 Windows 사용자가 JDK를 별도로 설치하지 않아도 실행할 수 있도록 `jpackage`를 사용해 배포 가능하게 만든다.

목표 결과물:

```text
CodexDesktop.exe
```

또는 Windows installer:

```text
CodexDesktop-Setup.exe
```

가능하면 custom Java Runtime까지 포함해서 배포한다.

# Codex 실행 환경

Codex CLI는 Windows가 아니라 **현재 WSL Ubuntu에 설치되어 있는 Codex CLI를 그대로 사용한다.**

Java에서는 `ProcessBuilder`를 사용해 `wsl.exe`를 실행한다.

기본 개념은 다음과 같다.

```java
ProcessBuilder builder = new ProcessBuilder(
    "wsl.exe",
    "-d",
    "Ubuntu",
    "--",
    "codex",
    "app-server",
    "--stdio"
);
```

프로젝트 디렉터리가 지정되어 있을 경우 WSL working directory도 해당 경로로 맞춘다.

예:

Windows 경로:

```text
C:\Users\USER\Desktop\dev\project
```

WSL 경로:

```text
/mnt/c/Users/USER/Desktop/dev/project
```

실행 예:

```text
wsl.exe -d Ubuntu --cd /mnt/c/Users/USER/Desktop/dev/project -- codex app-server --stdio
```

현재 설치된 Codex 실행 경로를 자동 탐색할 수 있으면 자동 탐지한다.

필요하면 설정에서 직접 입력할 수 있도록 한다.

예:

```text
codex
```

또는:

```text
/home/user/.codex/packages/standalone/current/codex
```

# Windows와 WSL 역할 분리

역할을 명확히 나눈다.

Windows JavaFX 애플리케이션:

```text
Windows
├─ GUI
├─ 프로젝트 목록
├─ Thread 목록
├─ 채팅 표시
├─ Codex 작업 상태 표시
├─ Tool Card
├─ Diff 표시
├─ 설정
└─ wsl.exe 프로세스 관리
```

실제 작업:

```text
WSL Ubuntu
├─ Codex
├─ bash
├─ git
├─ grep
├─ find
├─ java
├─ node
├─ npm
├─ mvn
└─ 파일 읽기 / 수정
```

Windows Java 애플리케이션에서 Codex의 shell 기능이나 파일 수정 기능을 재구현하지 않는다.

실제 명령 실행과 코드 작업은 Codex가 WSL에서 수행한다.

JavaFX 앱은 Codex 이벤트를 받아 사용자에게 보기 좋게 표현하는 역할을 한다.

# Codex app-server

가능하면 다음 방식을 사용한다.

```bash
codex app-server --stdio
```

터미널 ANSI 출력 자체를 파싱하는 방식보다 app-server의 구조화된 프로토콜을 사용한다.

단, 프로토콜을 추측해서 만들지 않는다.

구현 전에 현재 설치된 Codex를 직접 조사한다.

먼저 Windows에서 다음을 실행한다.

```powershell
wsl --list --verbose
wsl -d Ubuntu -- codex --version
wsl -d Ubuntu -- codex --help
wsl -d Ubuntu -- codex app-server --help
```

필요하면 현재 Codex 설치 디렉터리와 protocol schema/source도 조사한다.

현재 설치된 Codex 버전이 실제 사용하는 프로토콜에 맞춰 구현한다.

# Java 프로세스 관리

Codex 프로세스 실행은 Java `ProcessBuilder`로 구현한다.

별도 클래스로 분리한다.

예:

```text
WslCodexHost
```

역할:

```text
JavaFX
   ↓
WslCodexHost
   ↓
ProcessBuilder
   ↓
wsl.exe
   ↓
codex app-server --stdio
```

필수 구현:

* stdin redirect
* stdout redirect
* stderr redirect
* UTF-8 처리
* 비동기 stream 처리
* stdout/stderr deadlock 방지
* 프로세스 종료 감지
* graceful shutdown
* 강제 종료 fallback
* 앱 종료 시 프로세스 정리

중요:

JavaFX Application Thread에서 `readLine()` 같은 blocking I/O를 실행하지 않는다.

Virtual Thread 또는 별도 Executor를 활용해도 좋다.

Java 21 기능을 적극적으로 활용해도 된다.

예:

```java
Thread.ofVirtual().start(() -> {
    // stdout reader
});
```

단, UI 변경은 JavaFX Application Thread에서 수행한다.

필요하면:

```java
Platform.runLater(...)
```

를 사용한다.

# Codex 프로세스 관리 범위

앱을 종료할 때 이 애플리케이션이 직접 만든 Codex 프로세스만 관리한다.

다른 PowerShell, CMD 또는 WSL 터미널에서 사용자가 별도로 실행한 Codex 프로세스를 찾아서 종료하면 안 된다.

`pkill codex` 같은 광범위한 종료 명령은 사용하지 않는다.

현재 Java `Process` 객체 및 해당 프로세스 트리만 관리한다.

# 통신 계층

UI와 Codex 프로토콜 처리를 분리한다.

다음과 비슷한 구조로 만든다.

```text
codex/
├─ CodexHost.java
├─ WslCodexHost.java
├─ CodexAppServerClient.java
├─ CodexProtocolParser.java
├─ CodexSessionService.java
├─ CodexApprovalService.java
└─ CodexEventListener.java
```

인터페이스:

```java
public interface CodexHost {
    void start(...);
    void send(...);
    void stop();
    boolean isRunning();
}
```

초기 구현:

```text
CodexHost
└─ WslCodexHost
```

추후 다음과 같이 확장 가능하도록 한다.

```text
CodexHost
├─ WslCodexHost
├─ WindowsCodexHost
└─ RemoteCodexHost
```

과도한 추상화는 하지 않는다.

# JSON 처리

Codex app-server에서 전달되는 JSON 메시지는 Jackson을 이용해 처리한다.

예:

```text
CodexEvent
├─ type
├─ id
├─ method
├─ params
└─ result
```

실제 프로토콜 구조를 확인한 뒤 적절한 POJO 또는 record로 구현한다.

Java 21 record 사용을 권장한다.

예:

```java
public record CodexEvent(
    String type,
    JsonNode payload
) {}
```

모든 JSON 구조를 처음부터 강한 타입으로 박아 넣지 말고, 프로토콜 버전 변경을 고려해서 유연하게 설계한다.

# 프로젝트 경로 변환

Windows 경로를 WSL 경로로 변환하는 전용 Service를 만든다.

예:

```text
C:\Users\USER\Desktop\dev\VisitSeoul
```

↓

```text
/mnt/c/Users/USER/Desktop/dev/VisitSeoul
```

별도 클래스:

```text
WindowsWslPathConverter.java
```

코드 여기저기에서 문자열 replace하지 않는다.

가능하면 `wslpath` 명령을 이용하는 방식도 검토한다.

예:

```bash
wsl.exe -d Ubuntu -- wslpath "C:\Users\USER\Desktop\dev\VisitSeoul"
```

단 성능과 안정성을 고려해서 가장 적절한 방식을 선택한다.

# UI Framework

전체 GUI는 JavaFX로 구현한다.

Swing과 AWT UI 컴포넌트를 메인 UI에 사용하지 않는다.

필요한 경우 Windows native integration 정도만 별도로 사용할 수 있다.

UI는 FXML을 사용해도 되고 Java 코드 기반으로 만들어도 된다.

단, 유지보수성과 디자인 구현을 고려해서 적절히 선택한다.

스타일은 JavaFX CSS로 분리한다.

예:

```text
resources/
├─ css/
│  ├─ base.css
│  ├─ dark.css
│  └─ light.css
│
├─ fonts/
└─ icons/
```

# UI/UX 목표

UI 품질은 이 프로젝트에서 매우 중요하다.

기능만 동작하는 오래된 Java 프로그램처럼 보여서는 안 된다.

다음 제품 수준의 디자인을 참고한다.

```text
ChatGPT Desktop
Codex Desktop
Linear
VS Code
GitHub Desktop
JetBrains IDE
```

단 특정 앱을 그대로 복제하지는 않는다.

전체적으로:

* 현대적
* 미니멀
* 개발자 도구 느낌
* 빠르고 가벼움
* 여백이 충분함
* 정보 밀도가 적절함

을 목표로 한다.

# JavaFX 디자인 요구사항

기본 JavaFX Control을 그대로 배치한 것처럼 보이면 안 된다.

JavaFX CSS를 적극 활용한다.

다음 디자인 방향을 사용한다.

* rounded corner
* subtle border
* dark/light theme
* muted background
* compact controls
* hover state
* pressed state
* selected state
* focus state
* 적절한 typography hierarchy
* 충분한 whitespace
* 과도한 shadow 금지
* gradient 남발 금지
* accent color 남발 금지
* card UI 남발 금지
* animation 최소화

전체 UI의 기본 스타일은 ChatGPT Desktop / Codex Desktop처럼 깔끔하게 만든다.

**Do NOT design it like an old Swing application or traditional enterprise Java desktop application.**

# 폰트

Windows에서는 기본적으로 다음 우선순위를 고려한다.

```text
Segoe UI Variable
Segoe UI
System Default
```

코드 영역에는:

```text
Cascadia Code
Consolas
Monospace
```

순으로 fallback 가능하도록 한다.

# 전체 레이아웃

다음과 비슷하게 만든다.

```text
┌─────────────────────────────────────────────────────────────┐
│ Codex                                             ─ □ ×     │
├──────────────┬──────────────────────────────────────────────┤
│              │                                              │
│ + New Thread │ User                                         │
│              │ SearchService 페이징 확인해줘                 │
│ PROJECTS     │                                              │
│              │ ● Exploring project                          │
│ VisitSeoul   │                                              │
│ BearWorld    │ ┌────────────────────────────────────────┐   │
│ Personal     │ │ Read                                   │   │
│              │ │ SearchService.java                     │   │
│ THREADS      │ └────────────────────────────────────────┘   │
│              │                                              │
│ Today        │ limit 파라미터가 남아있는 부분을             │
│ Search fix   │ 확인했습니다.                                │
│ Archive      │                                              │
│              │ Modified                                     │
│              │ SearchService.java                    +3 -3  │
│              │                                              │
│              │                                              │
│              │ ┌──────────────────────────────────────────┐ │
│              │ │ Ask Codex...                         ↑   │ │
│              │ └──────────────────────────────────────────┘ │
├──────────────┴──────────────────────────────────────────────┤
│ ● WSL Ubuntu    ● Codex Connected      main       +3 -3    │
└─────────────────────────────────────────────────────────────┘
```

# Sidebar

Sidebar에는 다음을 표시한다.

```text
+ New Thread

PROJECTS
VisitSeoul
BearWorld
Personal

THREADS
Search pagination
Archive
Stamp feature

Settings
```

Sidebar 너비는 과도하게 넓지 않게 한다.

접고 펼칠 수 있도록 구현해도 된다.

선택된 프로젝트와 Thread는 명확하지만 과하지 않게 표시한다.

# 프로젝트 관리

지원:

* 프로젝트 추가
* 프로젝트 제거
* 최근 프로젝트
* 프로젝트 다시 열기
* 프로젝트별 마지막 Thread 기억

JavaFX `DirectoryChooser`를 이용해 Windows 디렉터리를 선택한다.

예:

```text
C:\Users\USER\Desktop\dev\VisitSeoul
```

선택 후 내부적으로 WSL 경로로 변환한다.

프로젝트 정보는 JSON 등의 간단한 로컬 설정 파일에 저장한다.

DB까지 사용할 필요는 없다.

# Thread

지원 목표:

* New Thread
* Thread 목록
* Thread 다시 열기
* 프로젝트별 Thread
* 현재 Thread 표시

가능하면 Codex 자체 Thread/Session 기능을 활용한다.

자체적으로 Codex conversation을 재구현하지 않는다.

현재 app-server에서 제공하는 기능을 먼저 조사한다.

# Conversation UI

채팅은 다음을 지원한다.

* User Message
* Assistant Message
* Streaming
* Markdown
* 코드 블록
* inline code
* 목록
* 링크
* 코드 Copy
* 자동 스크롤

중요:

모든 메시지를 큰 말풍선으로 만들지 않는다.

Assistant 메시지는 문서형 레이아웃처럼 자연스럽게 보여준다.

User 메시지는 약간 구분해도 된다.

ChatGPT/Codex UI와 비슷한 정보 밀도를 목표로 한다.

# Markdown

가능하면 Markdown을 직접 정규식으로 대충 구현하지 않는다.

성능이 괜찮은 Java Markdown parser 사용을 검토한다.

다만 Markdown 렌더링 하나를 위해 WebView 전체를 사용하지 않는다.

JavaFX Node 기반 renderer를 우선 검토한다.

최소 지원:

* heading
* paragraph
* bold
* italic
* inline code
* fenced code
* list
* link
* blockquote

# Composer

하단 입력 영역:

```text
┌─────────────────────────────────────────────┐
│ Ask Codex...                                │
│                                             │
│                                      ↑ Send │
└─────────────────────────────────────────────┘
```

지원:

* Enter 전송
* Shift+Enter 줄바꿈
* multiline
* Send
* Stop
* focus shortcut

작업 중에는 Send 대신 Stop 버튼으로 바뀌어도 된다.

# Streaming

Codex 응답 스트리밍이 올 때 token 또는 delta마다 전체 ListView를 다시 생성하지 않는다.

현재 Assistant Message의 Text Node만 효율적으로 업데이트한다.

UI update batching/debounce를 검토한다.

예를 들어 스트리밍 이벤트가 매우 자주 들어오면 16~50ms 정도 단위로 UI 반영을 합치는 방식을 사용할 수 있다.

단 응답이 눈에 띄게 늦어지면 안 된다.

# Codex 상태

Codex가 작업 중일 때 실제 이벤트 기반 상태를 표시한다.

예:

```text
Thinking
Exploring project
Reading files
Searching
Running command
Editing files
Reviewing changes
Writing response
```

spinner 하나만 계속 돌리지 않는다.

# Tool Call UI

Codex 작업은 compact card 형태로 표시한다.

예:

```text
Read
SearchService.java
```

```text
Command
git status                         ✓
```

```text
Modified
SearchController.java          +4 -2
```

기본적으로 작게 표시한다.

클릭하면 상세 내용을 펼칠 수 있다.

# Command 결과

긴 command output은 기본적으로 접는다.

예:

```text
Command
mvn test                          ✓

View output
```

클릭 시:

```text
[INFO] Building...
...
BUILD SUCCESS
```

형태로 출력한다.

긴 output은 UI 전체에 한꺼번에 Node 수천 개를 만들지 않는다.

TextArea 또는 virtualized component 등을 검토한다.

# 파일 변경

Codex가 수정한 파일을 표시한다.

예:

```text
SearchService.java
+12 -4
```

클릭하면 Diff를 표시한다.

최소 Unified Diff를 지원한다.

```diff
- limit=12
+ pagelength=12
```

가능하면 syntax highlighting도 적용한다.

# Git

프로젝트가 Git Repository인 경우 다음 정보도 표시하면 좋다.

```text
main
3 files changed
+12 -4
```

Git 관련 작업 역시 가능하면 WSL에서 수행한다.

예:

```bash
git status --porcelain
git diff --numstat
git diff
```

Windows Git을 별도로 요구하지 않는다.

# Approval

Codex app-server에서 approval 요청이 발생하면 GUI에 표시한다.

예:

```text
Codex wants to run:

mvn clean package

[ Allow ]
[ Allow for session ]
[ Deny ]
```

반드시 실제 Codex approval protocol을 사용한다.

자체적으로 approval 상태를 만들어서 Codex 보안 정책을 우회하지 않는다.

# Stop

작업 중 Stop 버튼을 누르면 현재 Turn을 중단한다.

앱 전체를 죽이거나 매번 Codex 프로세스를 종료하는 방식은 최후의 fallback으로만 사용한다.

app-server에 공식 interrupt/cancel 기능이 있으면 그것을 사용한다.

# 상태 표시

하단 상태바 등에 다음 정보를 표시한다.

```text
● WSL Ubuntu
● Codex Connected
main
3 files changed
```

연결 실패:

```text
● Codex Disconnected
```

연결 중:

```text
○ Connecting...
```

# Settings

설정 화면:

```text
Runtime

Mode
WSL

WSL executable
wsl.exe

Distribution
Ubuntu

Codex executable
codex

Codex arguments
app-server --stdio
```

추가:

```text
Appearance

Theme
System
Light
Dark
```

```text
Application

Debug Logging
Off / On
```

# Diagnostics

설정에서 환경 진단 기능을 제공한다.

예:

```text
WSL
✓ Installed

Ubuntu
✓ Available

Codex
✓ Installed

Version
codex-cli x.x.x

Authentication
✓ Ready

App Server
✓ Available
```

실패 시:

```text
Codex
✕ Command not found

Command:
wsl.exe -d Ubuntu -- codex --version
```

처럼 원인을 확인할 수 있게 한다.

# 성능

이 애플리케이션을 만드는 핵심 이유 중 하나가 기존 데스크톱 환경보다 가볍게 Codex를 사용하기 위해서다.

성능을 매우 중요하게 고려한다.

필수:

* Electron 금지
* Chromium 전체 UI 금지
* WebView 남용 금지
* Swing 금지
* JavaFX Application Thread에서 blocking I/O 금지
* stdout/stderr async
* Virtual Thread 활용 검토
* 응답 스트리밍 batching
* 긴 대화 virtualization
* Tool output lazy rendering
* Diff lazy rendering
* 불필요한 animation 금지
* 불필요한 timer 금지
* event listener 누수 방지
* background thread 정상 종료
* memory leak 방지

Chat history가 길어져도 UI 렉이 크게 증가하지 않도록 설계한다.

# Logging

SLF4J + Logback을 이용해 로그를 남긴다.

예:

```text
logs/
codex-desktop-2026-09-10.log
```

기록 대상:

* 앱 시작
* 앱 종료
* WSL 시작
* Codex 시작
* 프로세스 PID
* 연결 성공/실패
* protocol parsing error
* 프로세스 crash
* unhandled exception

기본적으로 기록 금지:

* Authentication Token
* Cookie
* API Key
* Password
* 사용자 전체 Prompt
* Codex 전체 Response
* 전체 source code
* 민감한 environment variable

raw JSONL 기록은 Debug Logging을 켠 경우에만 허용한다.

# Exception Handling

다음 상황을 정상적으로 처리한다.

* WSL 설치 안 됨
* Ubuntu distribution 없음
* Codex 없음
* Codex login 안 됨
* app-server 미지원
* 프로젝트 폴더 없음
* WSL 경로 변환 실패
* Codex process crash
* malformed JSON
* stdout 종료
* stderr 오류
* 현재 Turn 취소
* 앱 종료 중 Codex 실행 중
* 프로젝트 삭제됨

하나의 오류 때문에 JavaFX Application 전체가 종료되지 않도록 한다.

# 프로젝트 구조

다음과 비슷하게 구성한다.

```text
CodexDesktop/
├─ pom.xml
│
├─ src/main/java/
│  └─ com/example/codexdesktop/
│
│     ├─ CodexDesktopApplication.java
│     │
│     ├─ controller/
│     │  ├─ MainController.java
│     │  ├─ ConversationController.java
│     │  ├─ SidebarController.java
│     │  └─ SettingsController.java
│     │
│     ├─ viewmodel/
│     │  ├─ MainViewModel.java
│     │  ├─ ConversationViewModel.java
│     │  └─ SettingsViewModel.java
│     │
│     ├─ model/
│     │  ├─ ChatMessage.java
│     │  ├─ CodexEvent.java
│     │  ├─ ToolCall.java
│     │  ├─ FileChange.java
│     │  ├─ ProjectInfo.java
│     │  └─ CodexThread.java
│     │
│     ├─ codex/
│     │  ├─ CodexHost.java
│     │  ├─ WslCodexHost.java
│     │  ├─ CodexAppServerClient.java
│     │  ├─ CodexProtocolParser.java
│     │  ├─ CodexSessionService.java
│     │  └─ CodexApprovalService.java
│     │
│     ├─ service/
│     │  ├─ ProjectService.java
│     │  ├─ SettingsService.java
│     │  ├─ WindowsWslPathConverter.java
│     │  └─ GitService.java
│     │
│     └─ util/
│
└─ src/main/resources/
   ├─ fxml/
   ├─ css/
   │  ├─ base.css
   │  ├─ dark.css
   │  └─ light.css
   ├─ icons/
   └─ images/
```

필요하면 더 적합한 구조로 수정해도 된다.

과도한 layer와 boilerplate는 만들지 않는다.

# 개발 순서

처음부터 모든 기능을 한 번에 대충 만들지 않는다.

실제 실행 가능한 상태를 유지하면서 단계적으로 구현한다.

## Phase 0 - 환경 조사

먼저 다음을 실행한다.

```powershell
wsl --list --verbose
wsl -d Ubuntu -- codex --version
wsl -d Ubuntu -- codex --help
wsl -d Ubuntu -- codex app-server --help
```

확인:

* WSL distribution 이름
* Codex executable 경로
* Codex version
* app-server 지원 여부
* stdio transport
* initialize 방식
* thread 생성 방식
* turn 시작 방식
* streaming event
* approval event
* interrupt/cancel
* thread resume

프로토콜을 추측하지 않는다.

조사한 결과를 간단히 정리한 뒤 구현한다.

## Phase 1 - JavaFX Shell

먼저 UI 기본 구조를 만든다.

* Main Window
* Sidebar
* Conversation
* Composer
* Status bar
* Dark Theme

이 단계부터 기본적인 UI 품질을 갖춘다.

임시 Swing 느낌 UI를 만들어 놓고 나중에 바꾸는 방식은 피한다.

## Phase 2 - WSL 연결

구현:

```text
JavaFX
→ ProcessBuilder
→ wsl.exe
→ codex app-server --stdio
```

* Codex 실행
* stdin
* stdout
* stderr
* 연결 상태
* 프로세스 종료

실제로 연결되는 것을 확인한다.

## Phase 3 - 기본 Codex Chat

구현:

* Project 선택
* Thread 생성
* User Message
* Turn 시작
* Assistant Streaming
* Conversation UI
* Stop

여기까지 실제 Codex와 대화 가능해야 한다.

## Phase 4 - Tool UI

구현:

* Read
* Search
* Command
* File Edit
* Status
* Tool card

## Phase 5 - Approval

구현:

* approval request
* allow
* allow for session
* deny

## Phase 6 - File Changes / Git

구현:

* changed files
* +N / -N
* Git status
* Diff

## Phase 7 - Sessions

구현:

* New Thread
* Thread History
* Resume
* Project별 Thread

## Phase 8 - Settings / Diagnostics

구현:

* WSL 설정
* Distribution 설정
* Codex executable
* Theme
* Logging
* Connection diagnostics

## Phase 9 - Packaging

Windows용 jpackage를 구성한다.

최종적으로 Windows 사용자가 다음처럼 사용할 수 있어야 한다.

```text
CodexDesktop.exe
```

더블클릭:

```text
앱 실행
→ JavaFX UI
→ WSL Codex 자동 연결
→ 프로젝트 선택
→ Codex 사용
```

JDK를 별도로 설치할 필요가 없도록 packaged runtime을 포함한다.

# UI 품질 검수

기능 구현이 끝난 후 UI를 반드시 다시 검수한다.

다음 문제가 있으면 수정한다.

* JavaFX 기본 버튼 느낌이 강함
* Swing 앱처럼 보임
* 사내 업무 프로그램처럼 보임
* 모든 컴포넌트가 박스 안에 있음
* Sidebar가 너무 큼
* Toolbar가 너무 큼
* 폰트 hierarchy가 없음
* whitespace 부족
* 버튼이 지나치게 많음
* Tool Card가 너무 큼
* 모든 영역에 Border가 있음
* 색상을 지나치게 많이 사용함
* 로딩 animation이 과함
* 화면 크기 변경 시 layout이 깨짐
* 긴 대화에서 렉이 발생함

최종적으로 **개발자가 하루 종일 켜놓고 사용해도 부담 없는 현대적인 개발자 도구**처럼 보여야 한다.

# 완료 기준

단순히 build가 성공했다고 완료하지 않는다.

최소 다음 흐름을 실제로 검증한다.

```text
CodexDesktop 실행

→ WSL Ubuntu 탐지

→ Codex CLI 탐지

→ codex app-server 시작

→ Project 선택

→ Thread 생성

→ "이 프로젝트 구조 분석해줘" 입력

→ Codex 응답 Streaming 확인

→ Codex가 파일 읽는 이벤트 확인

→ Command 실행 표시 확인

→ 파일 수정 요청

→ Modified 표시 확인

→ Diff 확인

→ Stop 동작 확인

→ 앱 정상 종료

→ 앱이 생성한 Codex process 종료 확인
```

# 개발 과정에서의 행동

사소한 결정마다 사용자에게 질문하지 않는다.

예를 들어:

* 클래스명
* CSS spacing
* 아이콘 위치
* 작은 UI 구조
* package 이름
* 내부 구현 방식

등은 프로젝트 목적에 맞는 합리적인 선택을 직접 하고 진행한다.

다만 Codex app-server 프로토콜처럼 **추측해서 구현하면 실제 동작에 영향을 주는 부분은 반드시 현재 환경을 조사해서 결정한다.**

Mock 데이터만 만들어놓고 실제 Codex와 연결했다고 하지 않는다.

각 Phase마다:

```text
구현
→ Maven Build
→ 실행
→ 실제 동작 확인
→ 에러 수정
→ 다음 Phase
```

순서로 진행한다.

컴파일 에러나 명백한 런타임 오류를 남긴 채 다음 단계로 넘어가지 않는다.

# 최종 목표

사용 경험은 다음과 같아야 한다.

```text
CodexDesktop.exe 실행
        ↓
JavaFX 기반의 깔끔한 Windows GUI 표시
        ↓
최근 프로젝트 또는 프로젝트 폴더 선택
        ↓
채팅 입력
        ↓
Java ProcessBuilder가 wsl.exe 실행
        ↓
WSL Ubuntu
        ↓
기존 codex app-server --stdio
        ↓
기존 Codex 로그인과 설정 그대로 사용
        ↓
Codex가 WSL에서 프로젝트 분석
        ↓
파일 검색 / 명령 실행 / 코드 수정
        ↓
JavaFX GUI에서 실시간 표시

Thinking
Reading
Searching
Command
Modified
Diff
Approval
        ↓
완료 결과 확인
```

이 앱은 새로운 AI 서비스를 만드는 프로젝트가 아니다.

**현재 WSL에서 빠르고 안정적으로 동작하는 Codex CLI를 그대로 사용하면서, ChatGPT/Codex Desktop 수준으로 사용하기 편하고 보기 좋은 JavaFX 기반 Windows 데스크톱 GUI를 만드는 프로젝트다.**

먼저 Phase 0의 환경 조사부터 직접 수행하고, 확인한 실제 Codex 환경과 프로토콜을 기준으로 Phase 1부터 구현을 시작해줘.

중간에 사소한 사항을 계속 질문하지 말고, 실제 동작 가능한 애플리케이션 완성을 목표로 계속 진행해줘.
